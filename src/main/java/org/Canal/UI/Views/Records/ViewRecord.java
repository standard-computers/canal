package org.Canal.UI.Views.Records;

import org.Canal.UI.Elements.LockeState;

/**
 * /RCS/$[RECORD_ID]
 */
public class ViewRecord extends LockeState {

    public ViewRecord() {
        super("View Record", "/RCS/$", false, true, false, true);

    }
}